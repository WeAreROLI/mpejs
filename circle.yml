machine:
  environment:
    USER_NAME: odub
    USER_EMAIL: oscardub@gmail.com
  node:
    version: 4.2.2
dependencies:
  override:
    - npm install --dev
test:
  override:
    - npm run build
    - npm run lint
    - npm run test
    - npm run docs:build
deployment:
  production:
    branch: master
    commands:
      - npm run docs:deploy
